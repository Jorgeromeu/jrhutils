[project]
name = "jrhutils"
version = "0.1.0"
description = "My personal utility functions library"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "einops>=0.6.0",
    "matplotlib>=3.3.0",
    "numpy>=1.19.0<1.24.0",
    "pillow>=8.0.0<10.0.0",
]

[project.optional-dependencies]
torch = ["torch>=1.13.0"]

