[project]
name = "jrhutils"
version = "0.1.0"
description = "My personal utility functions library"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "einops>=0.6.0",
    "matplotlib>=3.5.0",
    "moviepy>=2.2.1",
    "numpy>=1.21.0",
    "pillow>=8.0.0",
]

[project.optional-dependencies]
torch = ["torch>=1.13.0"]

